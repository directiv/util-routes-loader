DEPENDENCIES

module.exports = function(update, Template) {
  // TODO handle hot reloading

  return function(modules, Routes, Route, RouteDefault, RouteError, RouteNotFound) {

    INITIALIZATION

    return function() {
      return AST;
    };
  }
};
